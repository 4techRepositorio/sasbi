[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fourpro-shared"
version = "0.1.0"
description = "4Pro_BI — utilitários partilhados"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["fourpro_shared"]
