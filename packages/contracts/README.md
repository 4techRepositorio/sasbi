# fourpro-contracts

Modelos Pydantic compartilhados. Instalação local:

```bash
pip install -e packages/contracts
```

Consumido por `apps/api` e, no futuro, gerado/consumido pelo `apps/web` via OpenAPI ou pacote TS espelhado.
